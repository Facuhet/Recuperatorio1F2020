#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int GetInt(char []);

float GetFloat(char []);

void GetString(char [], char[]);

int ValidarNumerosEnteros(char []);

int ValidarCadenaDeCaracteres(char []);

float ValidarNumerosFlotantes(char []);
